<a href="#hashtag-anchor" id="up" class="hashtag-title"></a>
<a id="hashtag-anchor"></a>

<div id="posts-list">
<% i = 0%>
  <% @posts.each do |post| %>


  <% i += 1%>
    <div class="post-container background-color-<%=i%>">
      <section class="post-id">
        <%= post.id %>
      </section>
      <section class="post-text">
        <%= CGI.unescapeHTML(post.text) %>
      </section>
      <section class="post-username">
        <img src=<%= post.profile_image_url %> class="avatar" /> @<%= post.screen_name %>
      </section>
      <section class="post-picture">
        <% if post.media_url %>
          <img src=<%= post.media_url %> />
        <% end %>
      </section>
      <section class="post-created-at"><i class="fa fa-2x fa-<%=post.source%>"></i>
        <%= post.time_of_post.strftime("%a %b %d %l:%M %p") %>
      </section>
    </div>
  <% i = 0 if i == 4 %>
  <% end %>
</div>

<div id="last_post_id"><%= @posts.last.id %></div>

<h1 id="loading"></h1>
