<a href="#hashtag-anchor" id="up" class="hashtag-title"></a>
<a id="hashtag-anchor"></a>

<div id="posts-list">
<% i = 0%>
  <% @posts.each do |post| %>


  <% i += 1%>
    <div class="tweet-container background-color-<%=i%>">
      <section class="tweet-text">
        <%= CGI.unescapeHTML(post.text) %>
      </section>
      <section class="tweet-username">
        <img src=<%= post.profile_image_url %> class="avatar" /> @<%= post.screen_name %>
      </section>
      <section class="tweet-picture">
        <% if post.media_url %>
          <img src=<%= post.media_url %> />
        <% end %>
      </section>
      <section class="tweet-created-at"><i class="fa fa-2x fa-<%=post.source%>"></i>
        <%= post.time_of_post.strftime("%a %b %d %l:%M %p") %>
      </section>
    </div>
  <% i = 0 if i == 4 %>
  <% end %>
</div>

<div id="last_page_requested">1</div>

<h1 id="loading"></h1>
